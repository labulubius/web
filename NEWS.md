# Private News

`https://labulubius.com/news` uses the existing Supabase sign-in and `site_is_admin()` role. Requests to same-origin `/api/news` on Vercel relay over the existing Cloudflare Tunnel to the `web` host. Only the web host accesses FreshRSS at `127.0.0.1:8080` and its Docker PostgreSQL database; credentials stay in the root-readable `/etc/labulubius/news.env` EnvironmentFile of `labulubius-web.service`. The browser never receives API secrets.

The News sidebar supports creating/renaming/deleting FreshRSS categories and adding/editing/deleting subscriptions. Deleting a category **unsubscribes all feeds in it, including hidden feeds, and removes their articles**; unchecking a feed merely hides it from News. Empty categories are created through FreshRSS's own PHP DAO in the local `freshrss` container. Input is validated and only admin-authorized server requests can reach these functions. The source-selection JSON files live outside the checkout at `${NEWS_DATA_DIR:-~/.local/share/labulubius/news}/<user-id>.json` on the web host. Feed names/URLs and articles are owned by FreshRSS, not Supabase. No new Docker container or DNS hostname is required.

## Five-day article lifecycle

The installed `scripts/news-retention-install.sql` schema creates `public.news_entry_receipt` and stamps the **first database receipt time** on INSERT. Updates to FreshRSS `lastSeen`, publication dates, unread/starred flags cannot extend it. An hourly root-owned systemd timer (`labulubius-news-retention.timer`) runs the installed `/usr/local/sbin/labulubius-news-retention` wrapper, which sends root-owned `/usr/local/share/labulubius/news-retention-cleanup.sql` into `freshrss-postgres` via `docker exec -i`. Expired entries and attached tags are hard-deleted. Tombstone hashes of expired `(feed ID, GUID)` prevent re-import while the subscription exists; they disappear when it is deleted. Those hashes contain no article content, but persist for the lifetime of the subscription. A hash collision could reject a different item. No article database backups are scheduled: GitHub stores code, **not** the article database.

`/api/news` checks receipt timestamps server-side before returning articles. The hourly job physically deletes expired rows; its failures should be monitored via `systemctl status labulubius-news-retention.service` and `journalctl -u labulubius-news-retention.service`. Do not confuse publication time with time received. FreshRSS's own UI may briefly show expired items before the next hourly deletion, even though News filters them. The filter runs on each GReader batch; pages consisting solely of expired entries may require clicking Load more to reach surviving articles.

To replicate this installation on a fresh, **empty** FreshRSS database: verify schema and run `docker exec -i freshrss-postgres sh -c 'exec psql -X -v ON_ERROR_STOP=1 -U "$POSTGRES_USER" -d "$POSTGRES_DB"' < scripts/news-retention-install.sql`. Install both `scripts/news-retention-cleanup.sql` and `scripts/news-retention-hourly.sh` as root-owned files to the paths above. Deploy root-owned systemd service/timer units and enable the timer. The installer deliberately refuses nonempty entries without receipt timestamps; do not guess first receipt from publication or lastSeen. Rerunning on an already installed DB is supported. If the FreshRSS database is recreated later, reinstall schema **before** fetching any articles.

## Verification

- Logged out, `/api/news?view=feeds` returns 401. Admin-only actions are also rejected.
- Signed in as admin: create a category, add an RSS feed to it, check the source, save and reload; rename/move/delete operations reflect in FreshRSS.
- `/news` only fetches News data from `labulubius.com/api/news`; the tunnel host is only contacted server-to-server.
- Confirm `labulubius-news-retention.timer` is active, and check hourly run status. There should be no automatic article database backup; if backups are added later, their article contents need their own five-day lifecycle policy.
